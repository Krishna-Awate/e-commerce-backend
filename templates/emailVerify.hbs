<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            max-width: 600px;
            margin: auto;
            padding: 20px;
            background-color: #f9f9f9;
            text-align: center;
        }
        .content {
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            padding: 40px;
        }
        h1 {
            color: #333333;
            margin-top: 0;
        }
        p {
            color: #666666;
            margin-bottom: 20px;
        }
        .btn {
            display: inline-block;
            background-color: #4CAF50;
            color: #ffffff;
            text-decoration: none;
            padding: 15px 30px;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="content">
            <h1>{{title}}</h1>
            <p>Hello {{name}}</p>
            <p>Click on the below link to verify your email address</p>
            <p><a href="{{resetLink}}" class="btn" style="color:white;">Verify email address</a></p>
            <p>If you did not request, please ignore this email.</p>
        </div>
    </div>
</body>
</html>
